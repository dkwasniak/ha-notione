{
  "entity": {
    "binary_sensor": {
      "moving": {
        "default": "mdi:bike",
        "state": {
          "on": "mdi:bike-fast",
          "off": "mdi:bike"
        }
      }
    },
    "sensor": {
      "last_seen": { "default": "mdi:clock-outline" },
      "geocode_city": { "default": "mdi:city" },
      "geocode_place": { "default": "mdi:map-marker" },
      "device_state": { "default": "mdi:radar" }
    },
    "switch": {
      "live": { "default": "mdi:crosshairs-gps" },
      "speed_alarm": { "default": "mdi:speedometer" },
      "battery_alarm": { "default": "mdi:battery-alert" }
    }
  }
}
